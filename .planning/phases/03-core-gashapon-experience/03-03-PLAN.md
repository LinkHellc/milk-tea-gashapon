---
phase: "03"
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-02"
files_modified:
  - pages/result/result.js
  - pages/result/result.wxml
  - pages/result/result.wxss
autonomous: true
requirements:
  - GASH-03

must_haves:
  truths:
    - "Result page plays capsule open animation on load"
    - "Pearl and milk tea name fade in after capsule opens"
    - "Result card animates in with slight bounce"
  artifacts:
    - path: "pages/result/result.js"
      provides: "Animation trigger on page load, result data display"
      exports: "onLoad, triggerResultAnimation"
    - path: "pages/result/result.wxml"
      provides: "Capsule animation elements, pearl, result card"
      exports: "animation capsule view, result card"
    - path: "pages/result/result.wxss"
      provides: "Capsule open keyframes, pearl animation, card bounce"
      exports: ".capsule-result, .pearl-bounce, .result-card-reveal"
---

<objective>
Implement the result reveal animation on the result page. When the page loads, the capsule that was already shown on the gashapon page animates to open and reveal the pearl + milk tea name, then the result card animates in.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update result.wxss with animation styles</name>
  <files>pages/result/result.wxss</files>
  <action>
Read the existing pages/result/result.wxss first, then add the following CSS to the END of the file:

```css
/* ====== Result Animation Styles ====== */

/* Animation container - centered above result card */
.result-animation-container {
  position: relative;
  width: 200rpx;
  height: 200rpx;
  margin: 60rpx auto 0;
  display: flex;
  justify-content: center;
  align-items: center;
}

/* Capsule - transparent sphere for result reveal */
.result-capsule {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  background: radial-gradient(circle at 30% 30%, rgba(255,255,255,0.9) 0%, rgba(255,255,255,0.3) 50%, rgba(255,255,255,0.1) 100%);
  border: 2rpx solid rgba(255,255,255,0.5);
  box-shadow: 0 0 20rpx rgba(255,255,255,0.3), inset 0 0 20rpx rgba(255,255,255,0.2);
  position: absolute;
  opacity: 0;
  transform: scale(0);
}

/* Pearl inside capsule */
.result-pearl {
  width: 40rpx;
  height: 40rpx;
  border-radius: 50%;
  background: #F5A6A0;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0);
  opacity: 0;
}

/* Result name inside capsule */
.result-capsule-name {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0);
  font-size: 20rpx;
  font-weight: 600;
  color: #333333;
  text-align: center;
  white-space: nowrap;
  opacity: 0;
}

/* Result card reveal animation */
.result-card {
  width: 600rpx;
  background: linear-gradient(180deg, #fff 0%, #f9f9f9 100%);
  border-radius: 20rpx;
  padding: 60rpx 40rpx;
  margin-top: 40rpx;
  text-align: center;
  box-shadow: 0 4rpx 20rpx rgba(0,0,0,0.1);
  opacity: 0;
  transform: translateY(30rpx);
}

/* Animation keyframes */

/* Capsule pop in */
@keyframes capsule-popin {
  0% {
    opacity: 0;
    transform: scale(0);
  }
  60% {
    opacity: 1;
    transform: scale(1.1);
  }
  100% {
    opacity: 1;
    transform: scale(1);
  }
}

/* Capsule open - splits in middle */
@keyframes capsule-open-result {
  0% {
    transform: scale(1);
    border-radius: 50%;
  }
  40% {
    transform: scale(1.3);
    border-radius: 50%;
  }
  60% {
    transform: scale(1.3);
    border-radius: 30% 30% 50% 50%;
  }
  100% {
    transform: scale(0);
    border-radius: 50%;
    opacity: 0;
  }
}

/* Pearl bounce in */
@keyframes pearl-bounce {
  0% {
    opacity: 0;
    transform: translate(-50%, -50%) scale(0);
  }
  60% {
    opacity: 1;
    transform: translate(-50%, -50%) scale(1.3);
  }
  80% {
    transform: translate(-50%, -50%) scale(0.9);
  }
  100% {
    opacity: 1;
    transform: translate(-50%, -50%) scale(1);
  }
}

/* Result name fade in */
@keyframes result-name-fadein {
  0% {
    opacity: 0;
    transform: translate(-50%, -50%) scale(0.5);
  }
  100% {
    opacity: 1;
    transform: translate(-50%, -50%) scale(1);
  }
}

/* Result card reveal with bounce */
@keyframes card-reveal {
  0% {
    opacity: 0;
    transform: translateY(30rpx);
  }
  60% {
    opacity: 1;
    transform: translateY(-10rpx);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Pearl glow pulse */
@keyframes pearl-glow {
  0%, 100% {
    box-shadow: 0 0 10rpx rgba(245,166,160,0.5);
  }
  50% {
    box-shadow: 0 0 20rpx rgba(245,166,160,0.8);
  }
}

.pearl-glow {
  animation: pearl-glow 1.5s ease-in-out infinite;
}
```
  </action>
  <verify>
    automated: grep -c "capsule-popin" pages/result/result.wxss && grep -c "pearl-bounce" pages/result/result.wxss && grep -c "card-reveal" pages/result/result.wxss
  </verify>
  <done>Result page CSS added with capsule open and card reveal animations</done>
</task>

<task type="auto">
  <name>Task 2: Update result.wxml with animation elements</name>
  <files>pages/result/result.wxml</files>
  <action>
Read the existing pages/result/result.wxml first.

Replace the entire content with:

```xml
<view class="container" style="background: linear-gradient(180deg, #f8f8f8 0%, #fff5f5 100%);">

  <!-- Result reveal animation -->
  <view class="result-animation-container">
    <!-- Capsule -->
    <view class="result-capsule {{capsuleClass}}">
      <!-- Pearl inside -->
      <view class="result-pearl {{pearlClass}} {{showPearl ? 'pearl-glow' : ''}}"></view>
      <!-- Result name -->
      <view class="result-capsule-name {{nameClass}}">{{milkTea}}</view>
    </view>
  </view>

  <!-- Result card -->
  <view class="result-card {{cardClass}}">
    <text class="brand-label">{{brand}}</text>
    <text class="milk-tea-name">{{milkTea}}</text>
    <text class="description">{{description}}</text>
  </view>

  <!-- Pearl graphic below card -->
  <view class="pearl-graphic {{pearlGraphicClass}}">
    <view class="pearl-circle"></view>
  </view>

  <!-- User info section -->
  <view class="user-section" wx:if="{{userInfo}}">
    <image class="avatar" src="{{userInfo.avatarUrl}}"></image>
    <text class="nickname">{{userInfo.nickName}}</text>
  </view>

  <!-- Login prompt -->
  <view class="login-prompt" wx:if="{{showLoginPrompt}}">
    <text class="prompt-text">登录后可保存抽取记录</text>
    <button class="login-btn" bindtap="onLoginTap">立即登录</button>
  </view>

  <!-- Action buttons -->
  <view class="actions">
    <button class="action-btn retry-btn" bindtap="onRetryTap">再扭一次</button>
    <button class="action-btn share-btn" bindtap="onShareTap">分享结果</button>
  </view>

</view>
```
  </action>
  <verify>
    automated: grep -c "result-capsule" pages/result/result.wxml && grep -c "result-pearl" pages/result/result.wxml && grep -c "result-card" pages/result/result.wxml
  </verify>
  <done>Result page WXML updated with animation elements</done>
</task>

<task type="auto">
  <name>Task 3: Update result.js with animation sequence</name>
  <files>pages/result/result.js</files>
  <action>
Read the existing pages/result/result.js first.

Replace the entire content with:

```javascript
const auth = require('../../utils/auth.js');

Page({
  data: {
    userInfo: null,
    showLoginPrompt: false,
    milkTea: null,
    brand: null,
    description: '经典口味，浓郁奶香',
    capsuleClass: '',
    pearlClass: '',
    nameClass: '',
    cardClass: '',
    pearlGraphicClass: '',
    showPearl: false
  },

  onLoad(options) {
    // Set result data from URL params
    this.setData({
      milkTea: options.milkTea ? decodeURIComponent(options.milkTea) : '默认奶茶',
      brand: options.brandName ? decodeURIComponent(options.brandName) : '未知品牌',
      description: options.description ? decodeURIComponent(options.description) : '经典口味，浓郁奶香'
    });

    // Check auth
    const userInfo = auth.getStoredUserInfo();
    if (userInfo) {
      this.setData({ userInfo, showLoginPrompt: false });
    } else {
      this.setData({ showLoginPrompt: true });
    }

    // Trigger result animation sequence
    this.triggerResultAnimation();
  },

  triggerResultAnimation() {
    // Step 1: Capsule pops in
    setTimeout(() => {
      this.setData({ capsuleClass: 'capsule-popin' });
    }, 200);

    // Step 2: Capsule opens after 0.8s
    setTimeout(() => {
      this.setData({ capsuleClass: 'capsule-open-result', showPearl: true });
      // Pearl bounces in
      this.setData({ pearlClass: 'pearl-bounce' });
      // Name fades in
      this.setData({ nameClass: 'result-name-fadein' });
    }, 1000);

    // Step 3: Result card reveals
    setTimeout(() => {
      this.setData({ cardClass: 'card-reveal' });
    }, 1600);

    // Step 4: Pearl graphic below card appears
    setTimeout(() => {
      this.setData({ pearlGraphicClass: 'pearl-bounce' });
    }, 2000);
  },

  onLoginTap() {
    auth.requestAuth(
      (userInfo) => {
        this.setData({ userInfo, showLoginPrompt: false });
        wx.showToast({ title: '登录成功', icon: 'success' });
      },
      () => {
        wx.showToast({ title: '登录已取消', icon: 'none' });
      }
    );
  },

  onRetryTap() {
    wx.navigateBack({ delta: 1 });
  },

  onShareTap() {
    // Implemented in Phase 4
    wx.showToast({ title: '分享功能开发中', icon: 'none' });
  }
});
```
  </action>
  <verify>
    automated: grep -c "triggerResultAnimation" pages/result/result.js && grep -c "capsule-popin" pages/result/result.js && grep -c "card-reveal" pages/result/result.js
  </verify>
  <done>Result page JS updated with animation sequence on load</done>
</task>

</tasks>

<verification>
[ ] pages/result/result.wxss contains @keyframes capsule-popin, capsule-open-result, pearl-bounce, card-reveal
[ ] pages/result/result.wxml contains result-capsule, result-pearl, result-card elements
[ ] pages/result/result.js calls triggerResultAnimation on onLoad
[ ] Animation sequence: capsule pop (0.2s) → capsule open (1s) → card reveal (1.6s) → pearl graphic (2s)
[ ] Pearl color is #F5A6A0
</verification>

<success_criteria>
- Result page loads → capsule pops in (0.2s)
- Capsule opens, reveals pearl + milk tea name (1s)
- Result card slides up with bounce (1.6s)
- Pearl graphic below card pulses gently
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-gashapon-experience/03-SUMMARY.md`
</output>
