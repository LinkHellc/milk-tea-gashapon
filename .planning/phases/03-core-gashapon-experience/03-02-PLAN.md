---
phase: "03"
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - pages/gashapon/gashapon.js
  - pages/gashapon/gashapon.wxml
  - pages/gashapon/gashapon.wxss
autonomous: true
requirements:
  - GASH-01

must_haves:
  truths:
    - "Tapping '扭一下' triggers the capsule animation sequence"
    - "Cup tilts right-downward during suction phase"
    - "Capsule spirals up inside the straw"
    - "Capsule ejects above the cup and opens to reveal result"
    - "Animation runs in parallel with cloud function call"
  artifacts:
    - path: "pages/gashapon/gashapon.js"
      provides: "Animation trigger, cup tilt, capsule rise animation control"
      exports: "onGashaponTap, startAnimation, cloud call"
    - path: "pages/gashapon/gashapon.wxml"
      provides: "Capsule element positioned above cup"
      exports: "animation capsule view"
    - path: "pages/gashapon/gashapon.wxss"
      provides: "Cup tilt transform, capsule spiral animation keyframes"
      exports: ".cup-tilt, .capsule, @keyframes capsule-spiral"
---

<objective>
Implement the capsule animation on the gashapon page. When the user taps "扭一下", the cup tilts to simulate drinking posture, the capsule spirals up through the straw, ejects above the cup, and opens to reveal the result. Animation runs in parallel with the cloud probability call.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update gashapon.wxss with animation styles</name>
  <files>pages/gashapon/gashapon.wxss</files>
  <action>
Read the existing pages/gashapon/gashapon.wxss first, then add the following CSS to the END of the file (before any existing styles, replacing .container if it conflicts):

```css
/* ====== Animation Styles ====== */

/* Cup tilt animation - simulates drinking posture */
.cup-tilt {
  transform: rotate(15deg);
  transition: transform 0.2s ease-in;
}

/* Animation container - positioned above cup */
.animation-container {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  display: flex;
  justify-content: center;
  align-items: flex-start;
}

/* Capsule - transparent sphere */
.capsule {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  background: radial-gradient(circle at 30% 30%, rgba(255,255,255,0.9) 0%, rgba(255,255,255,0.3) 50%, rgba(255,255,255,0.1) 100%);
  border: 2rpx solid rgba(255,255,255,0.5);
  box-shadow: 0 0 20rpx rgba(255,255,255,0.3), inset 0 0 20rpx rgba(255,255,255,0.2);
  position: absolute;
  top: 80rpx;
  opacity: 0;
  transform: translateY(0) rotate(0deg) scale(0);
}

/* Pearl inside capsule */
.capsule-pearl {
  width: 40rpx;
  height: 40rpx;
  border-radius: 50%;
  background: #F5A6A0;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  opacity: 0;
}

/* Result reveal inside capsule */
.capsule-result {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
  opacity: 0;
}

.capsule-result .result-name {
  font-size: 20rpx;
  font-weight: 600;
  color: #333333;
  white-space: nowrap;
}

.capsule-result .result-pearl {
  width: 24rpx;
  height: 24rpx;
  border-radius: 50%;
  background: #F5A6A0;
  margin: 4rpx auto 0;
}

/* Animation keyframes */

/* Capsule spiral rise through straw */
@keyframes capsule-rise {
  0% {
    opacity: 0;
    transform: translateY(300rpx) rotate(0deg) scale(0.3);
  }
  15% {
    opacity: 1;
    transform: translateY(240rpx) rotate(180deg) scale(0.5);
  }
  50% {
    opacity: 1;
    transform: translateY(120rpx) rotate(540deg) scale(0.8);
  }
  80% {
    opacity: 1;
    transform: translateY(40rpx) rotate(720deg) scale(1);
  }
  100% {
    opacity: 1;
    transform: translateY(0) rotate(900deg) scale(1);
  }
}

/* Capsule eject - pop out above cup */
@keyframes capsule-eject {
  0% {
    opacity: 1;
    transform: translateY(0) rotate(900deg) scale(1);
  }
  100% {
    opacity: 1;
    transform: translateY(-80rpx) rotate(1080deg) scale(1.1);
  }
}

/* Capsule open - splits open */
@keyframes capsule-open {
  0% {
    transform: scale(1.1);
    border-radius: 50%;
  }
  40% {
    transform: scale(1.3);
    border-radius: 50%;
  }
  60% {
    transform: scale(1.3);
    border-radius: 30% 30% 50% 50%;
  }
  100% {
    transform: scale(0);
    border-radius: 50%;
  }
}

/* Pearl fade in */
@keyframes pearl-fadein {
  0% {
    opacity: 0;
    transform: translate(-50%, -50%) scale(0);
  }
  60% {
    opacity: 1;
    transform: translate(-50%, -50%) scale(1.2);
  }
  100% {
    opacity: 1;
    transform: translate(-50%, -50%) scale(1);
  }
}

/* Result name fade in */
@keyframes result-fadein {
  0% {
    opacity: 0;
    transform: translate(-50%, -50%) scale(0.5);
  }
  100% {
    opacity: 1;
    transform: translate(-50%, -50%) scale(1);
  }
}

/* Button loading state */
.gashapon-btn.loading {
  background: #cccccc;
  box-shadow: none;
}

.gashapon-btn.loading::after {
  content: '抽取中...';
}

/* Pulsing animation for loading state */
@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.6; }
}

.gashapon-btn.pulsing {
  animation: pulse 1s ease-in-out infinite;
}
```
  </action>
  <verify>
    <automated>grep -c "@keyframes capsule-rise" pages/gashapon/gashapon.wxss && grep -c "cup-tilt" pages/gashapon/gashapon.wxss</automated>
  </verify>
  <done>Gashapon page CSS added with cup tilt, capsule animation keyframes</done>
</task>

<task type="auto">
  <name>Task 2: Update gashapon.wxml with capsule animation elements</name>
  <files>pages/gashapon/gashapon.wxml</files>
  <action>
Read the existing pages/gashapon/gashapon.wxml first.

Replace the entire content with:

```xml
<view class="container" style="background: linear-gradient(180deg, #f8f8f8 0%, #fff5f5 100%);">

  <!-- Brand Dropdown Selector (D-11) -->
  <view class="brand-selector" bindtap="toggleDropdown">
    <text class="current-brand">{{currentBrand.name}}</text>
    <text class="arrow {{showDropdown ? 'arrow-up' : ''}}">▼</text>
  </view>

  <!-- Dropdown Panel -->
  <view wx:if="{{showDropdown}}" class="dropdown-panel">
    <view wx:for="{{brands}}" wx:key="id"
          class="dropdown-item {{item.id === currentBrand.id ? 'selected' : ''}}"
          bindtap="onBrandChange"
          data-brand-index="{{index}}">
      <text>{{item.name}}</text>
    </view>
  </view>

  <!-- Milk Tea Cup Machine with Animation -->
  <view class="machine-container" style="position: relative;">
    <!-- Cup tilt wrapper - animates when spinning -->
    <view class="{{isSpinning ? 'cup-tilt' : ''}}" style="transition: transform 0.2s ease-in;">
      <view class="machine-card" style="background: linear-gradient(180deg, {{currentBrand.color}} 0%, {{currentBrand.color}}dd 100%);">
        <!-- Cup Lid -->
        <view class="machine-cup-lid" style="background: linear-gradient(180deg, {{currentBrand.color}}bb 0%, {{currentBrand.color}}88 100%);"></view>
        <!-- Cup Body -->
        <view class="machine-cup-body">
          <!-- Large Logo Placeholder -->
          <view class="machine-logo-placeholder" style="background: rgba(255,255,255,0.9);">
            <text class="machine-initials" style="color: {{currentBrand.color}};">{{currentBrand.name.[0]}}</text>
          </view>
          <!-- Brand Name -->
          <text class="machine-brand-name">{{currentBrand.name}}</text>
          <!-- Hint Text -->
          <text class="machine-hint">点击下方按钮抽取</text>
        </view>
        <!-- Straw Hole (with straw visual) -->
        <view class="machine-straw-hole"></view>
        <view class="straw-visual" style="position: absolute; right: 40rpx; top: 60rpx; width: 16rpx; height: 80rpx; background: linear-gradient(180deg, {{currentBrand.color}}cc 0%, {{currentBrand.color}}88 100%); border-radius: 8rpx;"></view>
      </view>
    </view>

    <!-- Capsule Animation Layer - positioned above cup -->
    <view class="animation-layer" wx:if="{{isSpinning}}" style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; display: flex; justify-content: center; align-items: flex-start; pointer-events: none;">
      <!-- Capsule -->
      <view class="capsule {{capsuleClass}}" style="position: relative;">
        <!-- Pearl inside -->
        <view class="capsule-pearl {{showPearl ? 'pearl-visible' : ''}}"></view>
        <!-- Result reveal -->
        <view class="capsule-result {{showResult ? 'result-visible' : ''}}">
          <text class="result-name">{{resultMilkTea.name}}</text>
          <view class="result-pearl"></view>
        </view>
      </view>
    </view>
  </view>

  <!-- Action Area with CTA -->
  <view class="action-area">
    <button class="gashapon-btn {{isSpinning ? 'loading pulsing' : ''}}" bindtap="onGashaponTap" hover-class="btn-hover" disabled="{{isSpinning}}">
      {{isSpinning ? '抽取中...' : '扭一下'}}
    </button>
  </view>

  <!-- User Info (from Phase 1 auth) -->
  <view class="user-info" wx:if="{{userInfo}}">
    <image class="avatar" src="{{userInfo.avatarUrl}}"></image>
    <text class="nickname">{{userInfo.nickName}}</text>
  </view>

</view>
```
  </action>
  <verify>
    <automated>grep -c "animation-layer" pages/gashapon/gashapon.wxml && grep -c "capsule" pages/gashapon/gashapon.wxml && grep -c "isSpinning" pages/gashapon/gashapon.wxml</automated>
  </verify>
  <done>Gashapon page WXML updated with capsule animation layer</done>
</task>

<task type="auto">
  <name>Task 3: Update gashapon.js with animation sequence and cloud call</name>
  <files>pages/gashapon/gashapon.js</files>
  <action>
Read the existing pages/gashapon/gashapon.js first.

Replace the entire content with:

```javascript
const { brands } = require('../../data/brands.js');
const auth = require('../../utils/auth.js');

Page({
  data: {
    brands: brands,
    currentBrand: null,
    showDropdown: false,
    userInfo: null,
    isAuthorized: false,
    isSpinning: false,
    capsuleClass: '',
    showPearl: false,
    showResult: false,
    resultMilkTea: null
  },

  onLoad(options) {
    // Load brands data
    this.setData({ brands: brands });

    // Initialize current brand from URL param or default to first
    if (options.brandId) {
      const brand = brands.find(b => b.id === options.brandId);
      if (brand) {
        this.setData({ currentBrand: brand });
        wx.setNavigationBarTitle({ title: brand.name });
      }
    } else {
      this.setData({ currentBrand: brands[0] });
      wx.setNavigationBarTitle({ title: brands[0].name });
    }

    // Check existing session (Phase 1 auth pattern)
    const userInfo = auth.getStoredUserInfo();
    if (userInfo) {
      this.setData({ userInfo, isAuthorized: true });
    }
  },

  toggleDropdown() {
    if (this.data.isSpinning) return;  // Disable during animation
    this.setData({ showDropdown: !this.data.showDropdown });
  },

  onBrandChange(e) {
    if (this.data.isSpinning) return;  // Disable during animation
    const index = e.currentTarget.dataset.brandIndex;
    const brand = this.data.brands[index];
    this.setData({
      currentBrand: brand,
      showDropdown: false
    });
    wx.setNavigationBarTitle({ title: brand.name });
  },

  onGashaponTap() {
    if (this.data.isSpinning) return;

    // Auth check first
    if (!this.data.isAuthorized) {
      auth.requestAuth(
        (userInfo) => {
          this.setData({ userInfo, isAuthorized: true });
          this.startAnimation();
        },
        () => {
          // Allow even if auth cancelled
          this.startAnimation();
        }
      );
    } else {
      this.startAnimation();
    }
  },

  startAnimation() {
    this.setData({ isSpinning: true, capsuleClass: '', showPearl: false, showResult: false, resultMilkTea: null });

    // Start animation sequence
    // Step 1: Capsule rises through straw (after cup tilt)
    setTimeout(() => {
      this.setData({ capsuleClass: 'capsule-rise' });
    }, 200);

    // Step 2: Capsule ejects above cup
    setTimeout(() => {
      this.setData({ capsuleClass: 'capsule-eject' });
    }, 1200);

    // Step 3: Capsule opens, pearl and result visible
    setTimeout(() => {
      this.setData({ capsuleClass: 'capsule-open', showPearl: true, showResult: true });
    }, 1800);

    // Call cloud function to get result
    wx.cloud.callFunction({
      name: 'gashapon',
      data: {
        brandId: this.data.currentBrand.id
      },
      success: (res) => {
        if (res.result && res.result.success) {
          this.setData({ resultMilkTea: res.result.data.milkTea });
        } else {
          // Fallback to first milk tea of brand if error
          const defaultMilkTeas = {
            'yidiandian': { name: '波霸奶茶', description: '大颗珍珠，嚼劲十足' },
            'xichaha': { name: '多肉葡萄', description: '葡萄果肉饱满，清爽解腻' },
            'bawangchaji': { name: '伯牙绝弦', description: '茉莉雪芽，茶香浓郁' },
            'guming': { name: '桂花系列', description: '桂花乌龙，唇齿留香' },
            'chabaidao': { name: '茉莉奶绿', description: '茉莉花香，清新回甘' },
            'coco': { name: '奶茶三兄弟', description: '珍珠+布丁+椰果，超满足' },
            'koi': { name: '伯爵奶茶', description: '伯爵红茶，经典风味' },
            'ruixing': { name: '生椰拿铁', description: '生椰+咖啡，潮流之选' },
            'hushangayi': { name: '手打柠檬茶', description: '柠檬清香，真实果肉' }
          };
          this.setData({ resultMilkTea: defaultMilkTeas[this.data.currentBrand.id] || { name: '精选奶茶', description: '门店推荐' } });
        }
      },
      fail: (err) => {
        console.error('Cloud function call failed:', err);
        // Use fallback
        const defaultMilkTeas = {
          'yidiandian': { name: '波霸奶茶', description: '大颗珍珠，嚼劲十足' },
          'xichaha': { name: '多肉葡萄', description: '葡萄果肉饱满，清爽解腻' },
          'bawangchaji': { name: '伯牙绝弦', description: '茉莉雪芽，茶香浓郁' },
          'guming': { name: '桂花系列', description: '桂花乌龙，唇齿留香' },
          'chabaidao': { name: '茉莉奶绿', description: '茉莉花香，清新回甘' },
          'coco': { name: '奶茶三兄弟', description: '珍珠+布丁+椰果，超满足' },
          'koi': { name: '伯爵奶茶', description: '伯爵红茶，经典风味' },
          'ruixing': { name: '生椰拿铁', description: '生椰+咖啡，潮流之选' },
          'hushangayi': { name: '手打柠檬茶', description: '柠檬清香，真实果肉' }
        };
        this.setData({ resultMilkTea: defaultMilkTeas[this.data.currentBrand.id] || { name: '精选奶茶', description: '门店推荐' } });
      },
      complete: () => {
        // Navigate to result after animation completes
        setTimeout(() => {
          this.navigateToResult();
        }, 2500);
      }
    });
  },

  navigateToResult() {
    const milkTea = this.data.resultMilkTea || { name: '精选奶茶', description: '门店推荐' };
    wx.navigateTo({
      url: `/pages/result/result?brandId=${this.data.currentBrand.id}&brandName=${encodeURIComponent(this.data.currentBrand.name)}&milkTea=${encodeURIComponent(milkTea.name)}&description=${encodeURIComponent(milkTea.description || '')}`
    });
    // Reset spin state
    this.setData({ isSpinning: false, capsuleClass: '', showPearl: false, showResult: false });
  }
});
```
  </action>
  <verify>
    <automated>grep -c "wx.cloud.callFunction" pages/gashapon/gashapon.js && grep -c "capsule-rise" pages/gashapon/gashapon.js && grep -c "startAnimation" pages/gashapon/gashapon.js</automated>
  </verify>
  <done>Gashapon page JS updated with animation sequence and cloud function call</done>
</task>

</tasks>

<verification>
[ ] pages/gashapon/gashapon.wxss contains @keyframes capsule-rise, capsule-eject, capsule-open
[ ] pages/gashapon/gashapon.wxss defines .cup-tilt with rotate(15deg)
[ ] pages/gashapon/gashapon.wxml contains animation-layer with capsule element
[ ] pages/gashapon/gashapon.wxml binds isSpinning to button disabled state
[ ] pages/gashapon/gashapon.js calls wx.cloud.callFunction({ name: 'gashapon' })
[ ] pages/gashapon/gashapon.js triggers animation sequence with setTimeout
[ ] Animation runs in parallel with cloud call (not waiting for result before animating)
[ ] After animation, navigates to result page with milk tea data
</verification>

<success_criteria>
- Tap "扭一下" → cup tilts right-downward (15°)
- Capsule spirals up through straw (0.2s delay → 1s rise → 0.2s eject)
- Capsule opens above cup, reveals pearl + milk tea name
- Cloud function called in parallel with animation start
- Button shows "抽取中..." and is disabled during spin
- After animation (2.5s), navigates to result page with result data
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-gashapon-experience/03-02-SUMMARY.md`
</output>
