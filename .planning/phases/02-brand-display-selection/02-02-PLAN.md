---
phase: 02
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - pages/index/index.wxml
  - pages/index/index.wxss
  - pages/index/index.js
autonomous: true
requirements:
  - BRAND-01

must_haves:
  truths:
    - "Home screen displays all 9 brands with logo and name"
    - "Each brand appears as milk tea cup-shaped gashapon machine"
    - "Users can tap a brand card to navigate to gashapon page"
  artifacts:
    - path: "pages/index/index.wxml"
      provides: "Swiper carousel with 3 brand cards per page"
      exports: "bindtap handlers for brand navigation"
    - path: "pages/index/index.wxss"
      provides: "Milk tea cup CSS styling (cup lid, body, straw hole)"
      exports: "brand-card, cup-lid, cup-body, straw-hole classes"
    - path: "pages/index/index.js"
      provides: "Brand data loading and navigation logic"
      exports: "onBrandTap navigation to gashapon"
  key_links:
    - from: "pages/index/index.js"
      to: "data/brands.js"
      via: "require()"
      pattern: "require.*brands"
    - from: "pages/index/index.wxml"
      to: "pages/gashapon/gashapon"
      via: "wx.navigateTo"
      pattern: "wx.navigateTo.*gashapon"
---

<objective>
Implement the index page with a swiper carousel (D-08) displaying 9 brands as milk tea cup-shaped cards (D-09, D-10). Each card shows brand logo and name; tapping navigates to the gashapon page with brandId parameter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@pages/index/index.wxml (existing placeholder - replace with swiper)
@pages/index/index.wxss (existing - add milk tea cup CSS)
@pages/index/index.js (existing placeholder - add brand loading)
@data/brands.js (created in 02-01)
@.planning/phases/02-brand-display-selection/02-UI-SPEC.md (D-08 swiper layout, D-09 cup card dimensions, per-brand colors)
</context>

<interfaces>
<!-- From data/brands.js (created in Plan 01) -->
```javascript
// Brand object shape:
{
  id: 'yidiandian',      // brand identifier
  name: '一点点',         // display name
  color: '#71C685',      // brand primary color
  logo: '/assets/brands/default.png'  // logo image path
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement index.wxml with swiper and milk tea cup cards</name>
  <files>pages/index/index.wxml</files>
  <action>
Replace entire file content with:

```xml
<view class="container" style="background: linear-gradient(180deg, #f8f8f8 0%, #fff5f5 100%);">
  <!-- Header -->
  <view class="header">
    <text class="title">奶茶扭蛋机</text>
    <text class="subtitle">选择品牌，开始扭蛋</text>
  </view>

  <!-- Brand Swiper (D-08: 3 brands per screen, 3 screens) -->
  <swiper class="brand-swiper"
          indicator-dots="{{false}}"
          autoplay="{{false}}"
          circular="{{false}}"
          previous-margin="30rpx"
          next-margin="30rpx"
          bindchange="onSwiperChange">
    <block wx:for="{{brandPages}}" wx:for-item="page">
      <swiper-item class="brand-page">
        <view wx:for="{{page}}" class="brand-card-wrapper" bindtap="onBrandTap" data-brand="{{item}}">
          <!-- Milk Tea Cup Card (D-09) -->
          <view class="brand-card" style="background: linear-gradient(180deg, {{item.color}} 0%, {{item.color}}dd 100%);">
            <!-- Cup Lid -->
            <view class="cup-lid" style="background: linear-gradient(180deg, {{item.color}}bb 0%, {{item.color}}88 100%);"></view>
            <!-- Cup Body -->
            <view class="cup-body">
              <!-- Brand Logo Placeholder (colored circle with initials) -->
              <view class="brand-logo-placeholder" style="background: rgba(255,255,255,0.9);">
                <text class="brand-initials" style="color: {{item.color}};">{{item.name.[0]}}</text>
              </view>
              <!-- Brand Name -->
              <text class="brand-name" style="color: #333333;">{{item.name}}</text>
            </view>
            <!-- Straw Hole (D-09: semi-transparent circle) -->
            <view class="straw-hole"></view>
          </view>
        </view>
      </swiper-item>
    </block>
  </swiper>

  <!-- Custom Dot Indicators (D-08: circle dots) -->
  <view class="swiper-dots">
    <view wx:for="{{3}}" class="dot {{currentSwiperIndex === index ? 'active' : ''}}"></view>
  </view>
</view>
```
  </action>
  <verify>
    <automated>grep -c "swiper" pages/index/index.wxml && grep -c "brand-card" pages/index/index.wxml</automated>
  </verify>
  <done>Index page displays swiper with milk tea cup brand cards</done>
</task>

<task type="auto">
  <name>Task 2: Implement index.wxss with milk tea cup styling</name>
  <files>pages/index/index.wxss</files>
  <action>
Replace entire file content with:

```css
/* Container with gradient background (D-12) */
.container {
  padding: 30rpx;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  background: linear-gradient(180deg, #f8f8f8 0%, #fff5f5 100%);
}

/* Header */
.header {
  text-align: center;
  margin-bottom: 40rpx;
}

.title {
  font-size: 48rpx;
  font-weight: 600;
  color: #333333;
  display: block;
}

.subtitle {
  font-size: 24rpx;
  color: #999999;
  display: block;
  margin-top: 8rpx;
}

/* Brand Swiper */
.brand-swiper {
  width: 100%;
  height: 420rpx;
}

.brand-page {
  display: flex;
  flex-direction: row;
  justify-content: space-around;
  align-items: center;
}

/* Brand Card Wrapper (for tap target) */
.brand-card-wrapper {
  padding: 8rpx;
}

/* Milk Tea Cup Card (D-09: 220rpx x 320rpx) */
.brand-card {
  width: 220rpx;
  height: 320rpx;
  border-radius: 20rpx;
  position: relative;
  overflow: hidden;
  box-shadow: 0 8rpx 24rpx rgba(0,0,0,0.1);
}

/* Cup Lid (darker shade, rounded top) */
.cup-lid {
  height: 40rpx;
  width: 100%;
  border-radius: 20rpx 20rpx 0 0;
}

/* Cup Body (brand color gradient, centered content) */
.cup-body {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 16rpx;
}

/* Brand Logo Placeholder (colored circle) */
.brand-logo-placeholder {
  width: 100rpx;
  height: 100rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 16rpx;
}

.brand-initials {
  font-size: 36rpx;
  font-weight: 600;
}

/* Brand Name */
.brand-name {
  font-size: 28rpx;
  font-weight: 600;
  text-shadow: 0 1rpx 2rpx rgba(255,255,255,0.8);
}

/* Straw Hole (D-09: top-right semi-transparent circle) */
.straw-hole {
  width: 20rpx;
  height: 20rpx;
  border-radius: 50%;
  background: rgba(255,255,255,0.5);
  position: absolute;
  top: 50rpx;
  right: 30rpx;
}

/* Swiper Dots */
.swiper-dots {
  display: flex;
  flex-direction: row;
  justify-content: center;
  align-items: center;
  margin-top: 24rpx;
  gap: 8rpx;
}

.dot {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
  background: #cccccc;
}

.dot.active {
  background: #ffb7b2;
}
```
  </action>
  <verify>
    <automated>grep -c "cup-lid" pages/index/index.wxss && grep -c "brand-card" pages/index/index.wxss</automated>
  </verify>
  <done>Index page CSS implements milk tea cup card styling with all required elements</done>
</task>

<task type="auto">
  <name>Task 3: Implement index.js with brand loading and navigation</name>
  <files>pages/index/index.js</files>
  <action>
Replace entire file content with:

```javascript
const { brands } = require('../../data/brands.js');

Page({
  data: {
    brandPages: [],    // brands split into pages of 3 for swiper
    currentSwiperIndex: 0
  },

  onLoad() {
    // Split brands into pages of 3 for swiper (D-08: 3 per screen)
    const pages = [];
    for (let i = 0; i < brands.length; i += 3) {
      pages.push(brands.slice(i, i + 3));
    }
    this.setData({ brandPages: pages });
  },

  onBrandTap(e) {
    const brand = e.currentTarget.dataset.brand;
    wx.navigateTo({
      url: `/pages/gashapon/gashapon?brandId=${brand.id}&brandName=${encodeURIComponent(brand.name)}`
    });
  },

  onSwiperChange(e) {
    this.setData({ currentSwiperIndex: e.detail.current });
  }
});
```
  </action>
  <verify>
    <automated>grep -c "brands" pages/index/index.js && grep -c "wx.navigateTo" pages/index/index.js</automated>
  </verify>
  <done>Index page loads brand data and navigates to gashapon on card tap</done>
</task>

</tasks>

<verification>
[ ] pages/index/index.wxml contains swiper with indicator-dots="{{false}}"
[ ] pages/index/index.wxml contains 9 brand cards rendered via wx:for over brandPages
[ ] pages/index/index.wxml contains custom dot indicators
[ ] pages/index/index.wxss defines .brand-card, .cup-lid, .cup-body, .straw-hole classes
[ ] pages/index/index.wxss defines gradient background per D-12
[ ] pages/index/index.js requires data/brands.js
[ ] pages/index/index.js implements onBrandTap with wx.navigateTo
[ ] pages/index/index.js splits brands into pages of 3 for swiper
</verification>

<success_criteria>
- Index page displays 9 brands in swiper carousel (3 per screen, 3 screens)
- Each brand renders as milk tea cup card with lid, body, straw hole
- Brand colors match per D-10 (一点点 green, 喜茶 pink, etc.)
- Tapping a card navigates to gashapon page with brandId and brandName
- Swiper dot indicators show current position and highlight active dot with accent color
</success_criteria>

<output>
After completion, create `.planning/phases/02-brand-display-selection/02-02-SUMMARY.md`
</output>
