---
phase: 01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.js
  - app.json
  - app.wxss
  - project.config.json
  - pages/index/index.wxml
  - pages/index/index.wxss
  - pages/index/index.js
  - pages/index/index.json
  - pages/gashapon/gashapon.wxml
  - pages/gashapon/gashapon.wxss
  - pages/gashapon/gashapon.js
  - pages/gashapon/gashapon.json
  - pages/result/result.wxml
  - pages/result/result.wxss
  - pages/result/result.js
  - pages/result/result.json
  - pages/profile/profile.wxml
  - pages/profile/profile.wxss
  - pages/profile/profile.js
  - pages/profile/profile.json
  - utils/auth.js
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "WeChat mini-program project structure exists and can be opened in DevTools"
    - "All 4 pages (index, gashapon, result, profile) are registered in app.json"
    - "Auth utilities exist with getStoredUserInfo, isAuthenticated, saveUserInfo, clearUserInfo, requestAuth"
  artifacts:
    - path: "app.json"
      provides: "Page registration for all 4 pages per D-07"
      contains: "pages/index/index"
    - path: "utils/auth.js"
      provides: "Authentication helper functions"
      exports: "getStoredUserInfo, isAuthenticated, saveUserInfo, clearUserInfo, requestAuth"
    - path: "pages/gashapon/gashapon.js"
      provides: "Lazy auth trigger on 扭一下 tap"
      contains: "onGashaponTap"
    - path: "pages/result/result.js"
      provides: "Avatar/nickname display per AUTH-02"
      contains: "userInfo"
  key_links:
    - from: "pages/gashapon/gashapon.js"
      to: "utils/auth.js"
      via: "require('../../utils/auth.js')"
      pattern: "require.*auth\\.js"
    - from: "utils/auth.js"
      to: "app.js"
      via: "Storage key namespace"
      pattern: "milkTeaGashapon_userInfo"
---

<objective>
Create the WeChat mini-program project scaffold: app entry files, all 4 page directories with skeleton files, and authentication utility module.

Purpose: Establish the foundational structure that downstream plans will implement against.
Output: Runnable mini-program skeleton with registered pages and auth helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-project-setup-authentication/01-CONTEXT.md
@.planning/phases/01-project-setup-authentication/01-RESEARCH.md

### From 01-RESEARCH.md — Project Structure Target
```
├── app.js                     # App entry, global state initialization
├── app.json                   # Pages registration, global styles, tabBar (none)
├── app.wxss                   # Global styles
├── project.config.json        # WeChat DevTools project config
├── pages/
│   ├── index/                 # Home page (brand selection)
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   ├── index.js
│   │   └── index.json
│   ├── gashapon/              # Gashapon machine page
│   │   ├── gashapon.wxml
│   │   ├── gashapon.wxss
│   │   ├── gashapon.js
│   │   └── gashapon.json
│   ├── result/                # Result display page
│   │   ├── result.wxml
│   │   ├── result.wxss
│   │   ├── result.js
│   │   └── result.json
│   └── profile/              # User profile page
│       ├── profile.wxml
│       ├── profile.wxss
│       ├── profile.js
│       └── profile.json
├── utils/
│   └── auth.js               # Auth helper per D-05
```

### From 01-RESEARCH.md — Auth Utility API
```javascript
module.exports = {
  getStoredUserInfo,    // returns userInfo or null
  isAuthenticated,       // returns boolean
  saveUserInfo,          // saves to Storage with namespaced key
  clearUserInfo,         // removes from Storage
  requestAuth             // wraps wx.getUserProfile with desc field
};
```

### From 01-RESEARCH.md — Storage Key Namespace
Use `'milkTeaGashapon_userInfo'` not just `'userInfo'` to avoid DevTools collision.

### From 01-CONTEXT.md — Page Structure (D-04)
- `pages/index/index` — 首页（品牌列表）
- `pages/gashapon/gashapon` — 抽取页（单个品牌扭蛋机）
- `pages/result/result` — 结果页
- `pages/profile/profile` — 个人中心

### From 01-CONTEXT.md — Project Name (D-06)
Project name: 奶茶扭蛋机
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app entry files and project config</name>
  <files>app.js, app.json, app.wxss, project.config.json</files>
  <read_first>None — creating new files</read_first>
  <behavior>
    - App entry with onLaunch checking for existing session
    - app.json registers all 4 pages in D-07 order
    - project.config.json sets appid and projectname per D-06
  </behavior>
  <action>
Create the following files with exact content:

**app.js** — App entry with session check:
```javascript
App({
  onLaunch() {
    const userInfo = wx.getStorageSync('milkTeaGashapon_userInfo');
    if (userInfo) {
      console.log('Existing session found for:', userInfo.nickName);
    }
  },
  globalData: {
    currentBrand: null
  }
});
```

**app.json** — Page registration per D-07:
```json
{
  "pages": [
    "pages/index/index",
    "pages/gashapon/gashapon",
    "pages/result/result",
    "pages/profile/profile"
  ],
  "window": {
    "navigationBarTitleText": "奶茶扭蛋机",
    "navigationBarBackgroundColor": "#f5f5f5"
  },
  "style": "v2",
  "sitemapLocation": "sitemap.json"
}
```

**app.wxss** — Global styles (minimal):
```css
/* Global reset */
page {
  background-color: #f5f5f5;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}

/* Utility classes */
.flex-center {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

**project.config.json** — DevTools config:
```json
{
  "description": "奶茶扭蛋机 - 微信小程序",
  "packOptions": {
    "ignore": []
  },
  "setting": {
    "urlCheck": false,
    "es6": true,
    "enhance": true,
    "postcss": true,
    "preloadBackgroundData": false,
    "minified": true,
    "newFeature": true,
    "coverView": true,
    "nodeModules": false,
    "autoAudits": false,
    "showShadowRootInWxmlPanel": true,
    "scopeDataCheck": false,
    "enableEngineNative": false,
    "useIsolateContext": true,
    "userConfirmedBundleSwitch": false,
    "packNpmManually": false,
    "packNpmRelationList": [],
    "minifyWXSS": true
  },
  "compileType": "miniprogram",
  "libVersion": "3.0.0",
  "appid": "touristappid",
  "projectname": "奶茶扭蛋机",
  "condition": {},
  "editorSetting": {
    "tabSize": 2,
    "insertSpace": false
  }
}
```
</action>
  <verify>
    <automated>grep -l "pages/index/index" app.json && grep -l "pages/gashapon/gashapon" app.json && grep -l "pages/result/result" app.json && grep -l "pages/profile/profile" app.json</automated>
  </verify>
  <acceptance_criteria>
    - [ ] app.json has all 4 pages registered in correct order
    - [ ] app.js exports App with onLaunch checking storage
    - [ ] project.config.json has "奶茶扭蛋机" as projectname
    - [ ] Storage key uses 'milkTeaGashapon_' prefix (not bare 'userInfo')
  </acceptance_criteria>
  <done>App entry files exist and can be opened in WeChat DevTools</done>
</task>

<task type="auto">
  <name>Task 2: Create index page skeleton</name>
  <files>pages/index/index.wxml, pages/index/index.wxss, pages/index/index.js, pages/index/index.json</files>
  <read_first>None — creating new files</read_first>
  <behavior>
    - Index page is home screen (brand list — Phase 2 content)
    - For Phase 1: skeleton shows page title, placeholder content
    - Page lifecycle hooks present
  </behavior>
  <action>
Create the following files for the index page:

**pages/index/index.wxml** — Page structure:
```xml
<view class="container">
  <view class="header">
    <text class="title">奶茶扭蛋机</text>
    <text class="subtitle">选择品牌，开始扭蛋</text>
  </view>
  <!-- Brand list will be implemented in Phase 2 -->
  <view class="placeholder">
    <text>品牌列表加载中...</text>
  </view>
</view>
```

**pages/index/index.wxss** — Styles:
```css
.container {
  padding: 30rpx;
  min-height: 100vh;
}

.header {
  text-align: center;
  margin-bottom: 40rpx;
}

.title {
  font-size: 40rpx;
  font-weight: bold;
  color: #333;
  display: block;
}

.subtitle {
  font-size: 28rpx;
  color: #999;
  display: block;
  margin-top: 10rpx;
}

.placeholder {
  text-align: center;
  padding: 100rpx 0;
  color: #ccc;
}
```

**pages/index/index.js** — Page logic:
```javascript
Page({
  data: {},

  onLoad(options) {
    // Phase 1: placeholder
    // Phase 2: load brand list
  },

  onShow() {
    // Refresh brand selection state
  }
});
```

**pages/index/index.json** — Page config:
```json
{
  "usingComponents": {},
  "navigationBarTitleText": "奶茶扭蛋机"
}
```
</action>
  <verify>
    <automated>ls pages/index/index.* | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - [ ] index.wxml exists with container and placeholder structure
    - [ ] index.js has Page({}) with onLoad and onShow hooks
    - [ ] index.json has navigationBarTitleText "奶茶扭蛋机"
  </acceptance_criteria>
  <done>Index page skeleton exists and renders without errors</done>
</task>

<task type="auto">
  <name>Task 3: Create gashapon page skeleton</name>
  <files>pages/gashapon/gashapon.wxml, pages/gashapon/gashapon.wxss, pages/gashapon/gashapon.js, pages/gashapon/gashapon.json</files>
  <read_first>None — creating new files</read_first>
  <behavior>
    - Gashapon page shows brand name and "扭一下" button
    - onGashaponTap handler exists (auth flow implemented in Wave 2)
    - Phase 1 skeleton: just the button and basic layout
  </behavior>
  <action>
Create the following files for the gashapon page:

**pages/gashapon/gashapon.wxml** — Page structure:
```xml
<view class="container">
  <view class="machine">
    <view class="machine-body">
      <text class="brand-name">{{currentBrand}}</text>
      <text class="machine-hint">点击下方按钮抽取</text>
    </view>
  </view>
  <view class="action-area">
    <button class="gashapon-btn" bindtap="onGashaponTap" hover-class="btn-hover">
      扭一下
    </button>
  </view>
  <view class="user-info" wx:if="{{userInfo}}">
    <image class="avatar" src="{{userInfo.avatarUrl}}"></image>
    <text class="nickname">{{userInfo.nickName}}</text>
  </view>
</view>
```

**pages/gashapon/gashapon.wxss** — Styles:
```css
.container {
  padding: 30rpx;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.machine {
  width: 400rpx;
  height: 500rpx;
  margin: 60rpx 0;
}

.machine-body {
  width: 100%;
  height: 100%;
  background: linear-gradient(180deg, #8B4513 0%, #D2691E 50%, #8B4513 100%);
  border-radius: 20rpx;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

.brand-name {
  font-size: 36rpx;
  color: #fff;
  font-weight: bold;
}

.machine-hint {
  font-size: 24rpx;
  color: rgba(255,255,255,0.7);
  margin-top: 20rpx;
}

.action-area {
  margin-top: 60rpx;
}

.gashapon-btn {
  width: 300rpx;
  height: 80rpx;
  background: linear-gradient(135deg, #FF6B6B 0%, #FF8E53 100%);
  color: #fff;
  font-size: 32rpx;
  font-weight: bold;
  border-radius: 40rpx;
  border: none;
}

.btn-hover {
  opacity: 0.8;
}

.user-info {
  display: flex;
  align-items: center;
  margin-top: 40rpx;
  padding: 20rpx;
}

.avatar {
  width: 60rpx;
  height: 60rpx;
  border-radius: 50%;
}

.nickname {
  margin-left: 20rpx;
  font-size: 28rpx;
  color: #666;
}
```

**pages/gashapon/gashapon.js** — Page logic (skeleton for Wave 2 implementation):
```javascript
const auth = require('../../utils/auth.js');

Page({
  data: {
    userInfo: null,
    isAuthorized: false,
    currentBrand: null,
    isSpinning: false
  },

  onLoad(options) {
    if (options.brandId) {
      this.setData({ currentBrand: options.brandId });
    }
    // Check existing session
    const userInfo = auth.getStoredUserInfo();
    if (userInfo) {
      this.setData({ userInfo, isAuthorized: true });
    }
  },

  onGashaponTap() {
    // Implemented in Wave 2 — placeholder for now
    console.log('onGashaponTap called');
  }
});
```

**pages/gashapon/gashapon.json** — Page config:
```json
{
  "usingComponents": {},
  "navigationBarTitleText": "抽取"
}
```
</action>
  <verify>
    <automated>ls pages/gashapon/gashapon.* | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - [ ] gashapon.wxml has "扭一下" button with bindtap="onGashaponTap"
    - [ ] gashapon.js has onGashaponTap handler (placeholder)
    - [ ] gashapon.js imports utils/auth.js
    - [ ] gashapon.wxss has .gashapon-btn styles
  </acceptance_criteria>
  <done>Gashapon page skeleton exists and renders without errors</done>
</task>

<task type="auto">
  <name>Task 4: Create result page skeleton</name>
  <files>pages/result/result.wxml, pages/result/result.wxss, pages/result/result.js, pages/result/result.json</files>
  <read_first>None — creating new files</read_first>
  <behavior>
    - Result page displays milk tea name and brand
    - Shows user avatar/nickname if logged in (per AUTH-02)
    - Shows login prompt if not logged in (per D-03)
  </behavior>
  <action>
Create the following files for the result page:

**pages/result/result.wxml** — Page structure:
```xml
<view class="container">
  <view class="result-card">
    <text class="brand-label">{{brand}}</text>
    <text class="milk-tea-name">{{milkTea}}</text>
    <text class="description">{{description}}</text>
  </view>

  <view class="user-section" wx:if="{{userInfo}}">
    <image class="avatar" src="{{userInfo.avatarUrl}}"></image>
    <text class="nickname">{{userInfo.nickName}}</text>
  </view>

  <view class="login-prompt" wx:if="{{showLoginPrompt}}">
    <text class="prompt-text">登录后可保存抽取记录</text>
    <button class="login-btn" bindtap="onLoginTap">立即登录</button>
  </view>

  <view class="actions">
    <button class="action-btn retry-btn" bindtap="onRetryTap">再扭一次</button>
    <button class="action-btn share-btn" bindtap="onShareTap">分享结果</button>
  </view>
</view>
```

**pages/result/result.wxss** — Styles:
```css
.container {
  padding: 30rpx;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.result-card {
  width: 600rpx;
  background: linear-gradient(180deg, #fff 0%, #f9f9f9 100%);
  border-radius: 20rpx;
  padding: 60rpx 40rpx;
  margin-top: 60rpx;
  text-align: center;
  box-shadow: 0 4rpx 20rpx rgba(0,0,0,0.1);
}

.brand-label {
  font-size: 24rpx;
  color: #999;
  display: block;
}

.milk-tea-name {
  font-size: 48rpx;
  font-weight: bold;
  color: #333;
  display: block;
  margin-top: 20rpx;
}

.description {
  font-size: 28rpx;
  color: #666;
  display: block;
  margin-top: 20rpx;
}

.user-section {
  display: flex;
  align-items: center;
  margin-top: 60rpx;
  padding: 20rpx;
  background: #fff;
  border-radius: 30rpx;
}

.avatar {
  width: 60rpx;
  height: 60rpx;
  border-radius: 50%;
}

.nickname {
  margin-left: 20rpx;
  font-size: 28rpx;
  color: #666;
}

.login-prompt {
  margin-top: 40rpx;
  text-align: center;
  padding: 30rpx;
  background: #fff8e6;
  border-radius: 12rpx;
}

.prompt-text {
  font-size: 26rpx;
  color: #996600;
}

.login-btn {
  margin-top: 20rpx;
  background: linear-gradient(135deg, #FF6B6B 0%, #FF8E53 100%);
  color: #fff;
  font-size: 28rpx;
  padding: 20rpx 60rpx;
  border-radius: 30rpx;
  border: none;
}

.actions {
  display: flex;
  gap: 30rpx;
  margin-top: 60rpx;
}

.action-btn {
  padding: 20rpx 40rpx;
  font-size: 28rpx;
  border-radius: 30rpx;
}

.retry-btn {
  background: #fff;
  color: #333;
  border: 2rpx solid #ddd;
}

.share-btn {
  background: linear-gradient(135deg, #4CAF50 0%, #81C784 100%);
  color: #fff;
  border: none;
}
```

**pages/result/result.js** — Page logic:
```javascript
const auth = require('../../utils/auth.js');

Page({
  data: {
    userInfo: null,
    showLoginPrompt: false,
    milkTea: null,
    brand: null,
    description: '经典口味，浓郁奶香'
  },

  onLoad(options) {
    this.setData({
      milkTea: options.milkTea || '默认奶茶',
      brand: options.brand || '未知品牌'
    });

    const userInfo = auth.getStoredUserInfo();
    if (userInfo) {
      this.setData({ userInfo, showLoginPrompt: false });
    } else {
      this.setData({ showLoginPrompt: true });
    }
  },

  onLoginTap() {
    auth.requestAuth(
      (userInfo) => {
        this.setData({ userInfo, showLoginPrompt: false });
        wx.showToast({ title: '登录成功', icon: 'success' });
      },
      () => {
        wx.showToast({ title: '登录已取消', icon: 'none' });
      }
    );
  },

  onRetryTap() {
    wx.navigateBack({ delta: 1 });
  },

  onShareTap() {
    // Implemented in Phase 4
    wx.showToast({ title: '分享功能开发中', icon: 'none' });
  }
});
```

**pages/result/result.json** — Page config:
```json
{
  "usingComponents": {},
  "navigationBarTitleText": "抽取结果"
}
```
</action>
  <verify>
    <automated>ls pages/result/result.* | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - [ ] result.wxml shows userInfo avatar/nickname when available (AUTH-02)
    - [ ] result.wxml shows login prompt when showLoginPrompt is true (D-03)
    - [ ] result.js imports utils/auth.js
    - [ ] result.js has onLoginTap handler calling auth.requestAuth
  </acceptance_criteria>
  <done>Result page skeleton exists and renders without errors</done>
</task>

<task type="auto">
  <name>Task 5: Create profile page skeleton with auth guard</name>
  <files>pages/profile/profile.wxml, pages/profile/profile.wxss, pages/profile/profile.js, pages/profile/profile.json</files>
  <read_first>None — creating new files</read_first>
  <behavior>
    - Profile page shows user avatar, nickname when logged in
    - Shows "未登录" state when not logged in
    - Auth guard redirects unauthenticated users (per Common Pitfalls #3)
  </behavior>
  <action>
Create the following files for the profile page:

**pages/profile/profile.wxml** — Page structure:
```xml
<view class="container">
  <block wx:if="{{userInfo}}">
    <view class="profile-header">
      <image class="avatar" src="{{userInfo.avatarUrl}}"></image>
      <text class="nickname">{{userInfo.nickName}}</text>
    </view>
    <view class="stats">
      <view class="stat-item">
        <text class="stat-value">{{drawCount}}</text>
        <text class="stat-label">抽取次数</text>
      </view>
      <view class="stat-item">
        <text class="stat-value">{{points}}</text>
        <text class="stat-label">积分</text>
      </view>
      <view class="stat-item">
        <text class="stat-value">{{achievementCount}}</text>
        <text class="stat-label">成就</text>
      </view>
    </view>
    <view class="menu-list">
      <view class="menu-item" bindtap="onHistoryTap">
        <text>抽取历史</text>
        <text class="arrow">></text>
      </view>
      <view class="menu-item" bindtap="onAchievementTap">
        <text>我的成就</text>
        <text class="arrow">></text>
      </view>
      <view class="menu-item logout" bindtap="onLogoutTap">
        <text>退出登录</text>
      </view>
    </view>
  </block>
  <block wx:else>
    <view class="not-logged">
      <image class="guest-avatar" src="/assets/default-avatar.png"></image>
      <text class="guest-text">未登录</text>
      <button class="login-btn" bindtap="onLoginTap">点击登录</button>
    </view>
  </block>
</view>
```

**pages/profile/profile.wxss** — Styles:
```css
.container {
  padding: 30rpx;
  min-height: 100vh;
}

.profile-header {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 60rpx 0;
}

.avatar {
  width: 160rpx;
  height: 160rpx;
  border-radius: 50%;
  border: 6rpx solid #fff;
  box-shadow: 0 4rpx 20rpx rgba(0,0,0,0.1);
}

.nickname {
  font-size: 36rpx;
  color: #333;
  font-weight: bold;
  margin-top: 20rpx;
}

.stats {
  display: flex;
  justify-content: space-around;
  background: #fff;
  border-radius: 16rpx;
  padding: 30rpx 0;
  margin-bottom: 40rpx;
}

.stat-item {
  text-align: center;
}

.stat-value {
  font-size: 40rpx;
  font-weight: bold;
  color: #FF6B6B;
  display: block;
}

.stat-label {
  font-size: 24rpx;
  color: #999;
  display: block;
  margin-top: 10rpx;
}

.menu-list {
  background: #fff;
  border-radius: 16rpx;
}

.menu-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 30rpx;
  border-bottom: 1rpx solid #f0f0f0;
  font-size: 28rpx;
  color: #333;
}

.menu-item:last-child {
  border-bottom: none;
}

.menu-item.logout {
  color: #FF6B6B;
  justify-content: center;
}

.not-logged {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 120rpx 0;
}

.guest-avatar {
  width: 160rpx;
  height: 160rpx;
  border-radius: 50%;
  opacity: 0.5;
}

.guest-text {
  font-size: 32rpx;
  color: #999;
  margin-top: 20rpx;
}

.login-btn {
  margin-top: 40rpx;
  background: linear-gradient(135deg, #FF6B6B 0%, #FF8E53 100%);
  color: #fff;
  font-size: 28rpx;
  padding: 20rpx 80rpx;
  border-radius: 30rpx;
  border: none;
}
```

**pages/profile/profile.js** — Page logic with auth guard:
```javascript
const auth = require('../../utils/auth.js');

Page({
  data: {
    userInfo: null,
    drawCount: 0,
    points: 0,
    achievementCount: 0
  },

  onLoad() {
    // Auth guard — check on load
    const userInfo = auth.getStoredUserInfo();
    if (!userInfo) {
      wx.showToast({ title: '请先登录', icon: 'none' });
      // Don't navigate back — show the not-logged state instead
      return;
    }
    this.setData({ userInfo });
    // Load user stats (from cloud in Phase 5, defaults for now)
    this.setData({
      drawCount: 0,
      points: 0,
      achievementCount: 0
    });
  },

  onShow() {
    // Re-check auth state when page shows (user may have logged in elsewhere)
    const userInfo = auth.getStoredUserInfo();
    this.setData({ userInfo: userInfo || null });
  },

  onLoginTap() {
    auth.requestAuth(
      (userInfo) => {
        this.setData({ userInfo });
        wx.showToast({ title: '登录成功', icon: 'success' });
      },
      () => {
        wx.showToast({ title: '登录已取消', icon: 'none' });
      }
    );
  },

  onHistoryTap() {
    wx.showToast({ title: '历史记录开发中', icon: 'none' });
  },

  onAchievementTap() {
    wx.showToast({ title: '成就系统开发中', icon: 'none' });
  },

  onLogoutTap() {
    wx.showModal({
      title: '确认退出',
      content: '确定要退出登录吗？',
      success: (res) => {
        if (res.confirm) {
          auth.clearUserInfo();
          this.setData({ userInfo: null });
          wx.showToast({ title: '已退出', icon: 'success' });
        }
      }
    });
  }
});
```

**pages/profile/profile.json** — Page config:
```json
{
  "usingComponents": {},
  "navigationBarTitleText": "个人中心"
}
```
</action>
  <verify>
    <automated>ls pages/profile/profile.* | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - [ ] profile.wxml shows avatar/nickname when userInfo exists (AUTH-02)
    - [ ] profile.wxml shows "未登录" state when userInfo is null
    - [ ] profile.js imports utils/auth.js
    - [ ] profile.js has onLoad auth guard with wx.showToast
    - [ ] profile.js has onLogoutTap calling auth.clearUserInfo()
  </acceptance_criteria>
  <done>Profile page skeleton exists and renders without errors</done>
</task>

<task type="auto">
  <name>Task 6: Create auth utility module</name>
  <files>utils/auth.js</files>
  <read_first>None — creating new files</read_first>
  <behavior>
    - Provides getStoredUserInfo, isAuthenticated, saveUserInfo, clearUserInfo, requestAuth
    - Uses 'milkTeaGashapon_' prefix for all Storage keys (per Pitfall #2)
    - requestAuth wraps wx.getUserProfile with proper desc field
  </behavior>
  <action>
Create **utils/auth.js** with the following content:

```javascript
/**
 * Auth utility functions for milk tea gashapon mini-program
 * Uses 'milkTeaGashapon_' prefixed Storage keys to avoid DevTools collisions
 */

const STORAGE_KEY = 'milkTeaGashapon_userInfo';

/**
 * Get stored user info
 * @returns {Object|null} userInfo or null if not logged in
 */
function getStoredUserInfo() {
  return wx.getStorageSync(STORAGE_KEY) || null;
}

/**
 * Check if user is authenticated
 * @returns {boolean}
 */
function isAuthenticated() {
  return !!getStoredUserInfo();
}

/**
 * Save user info to storage
 * @param {Object} userInfo - from wx.getUserProfile success callback
 */
function saveUserInfo(userInfo) {
  wx.setStorageSync(STORAGE_KEY, userInfo);
}

/**
 * Clear user info (logout)
 */
function clearUserInfo() {
  wx.removeStorageSync(STORAGE_KEY);
}

/**
 * Request user authorization via getUserProfile
 * @param {Function} success - callback(userInfo)
 * @param {Function} fail - callback() for when user cancels
 */
function requestAuth(success, fail) {
  wx.getUserProfile({
    desc: '用于展示您的头像和昵称',
    success: (res) => {
      saveUserInfo(res.userInfo);
      success(res.userInfo);
    },
    fail: () => {
      fail && fail();
    }
  });
}

module.exports = {
  getStoredUserInfo,
  isAuthenticated,
  saveUserInfo,
  clearUserInfo,
  requestAuth
};
```

Create **utils/auth.js** (the directory utils/ may need to be created first).
</action>
  <verify>
    <automated>test -f utils/auth.js && grep -l "getStoredUserInfo\|isAuthenticated\|saveUserInfo\|clearUserInfo\|requestAuth" utils/auth.js</automated>
  </verify>
  <acceptance_criteria>
    - [ ] utils/auth.js exports all 5 functions
    - [ ] Storage key uses 'milkTeaGashapon_userInfo' prefix
    - [ ] requestAuth calls wx.getUserProfile with desc field
    - [ ] clearUserInfo calls wx.removeStorageSync (not set(null))
  </acceptance_criteria>
  <done>Auth utility module exists with all required functions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> WeChat | wx.getUserProfile crosses here — user must explicitly authorize |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | auth.js | mitigate | WeChat auth dialog is system-level; no custom dialogs |
| T-01-02 | Information Disclosure | LocalStorage | accept | Only storing non-sensitive avatar/nickname; no session tokens |
| T-01-03 | Tampering | Auth bypass | accept | D-02 allows anonymous use — no enforcement needed |
</threat_model>

<verification>
## Phase 1 Scaffold Verification

1. **All files exist:**
   ```bash
   test -f app.js && test -f app.json && test -f app.wxss && test -f project.config.json
   test -f pages/index/index.js && test -f pages/gashapon/gashapon.js && test -f pages/result/result.js && test -f pages/profile/profile.js
   test -f utils/auth.js
   ```

2. **app.json pages registered:**
   ```bash
   grep "pages/index/index" app.json && grep "pages/gashapon/gashapon" app.json && grep "pages/result/result" app.json && grep "pages/profile/profile" app.json
   ```

3. **Storage key namespace:**
   ```bash
   grep "milkTeaGashapon_userInfo" utils/auth.js
   ```
</verification>

<success_criteria>
- [ ] WeChat mini-program project can be opened in DevTools
- [ ] All 4 pages render without errors
- [ ] Auth utilities work: getStoredUserInfo, saveUserInfo, clearUserInfo, requestAuth
- [ ] Page flow: index -> gashapon -> result -> profile works
- [ ] Login prompt appears on result page for unauthenticated users
- [ ] Profile page shows "未登录" state when not authenticated
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-authentication/01-01-SUMMARY.md`
</output>